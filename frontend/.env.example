# Copy to .env for local dev, or set as Environment Variable in Vercel.
# Local:
# VITE_API_URL=http://localhost:8082/api
# Production (after Render deploy — note the /api suffix):
VITE_API_URL=https://internlite-backend.onrender.com/api
